# Batch01 Backend / Conditional Closure v0.2

Status: `CURRENT_SOURCE_RECHECK_COMPLETE / TRAINING_MODE_QUEUE_REMAINS`  
Characters: Jamie / Mai / Zangief / Cammy  
Checked: 2026-09-17  
Version boundary: Year 4 / 2026-08-03 balance update; structured guides updated 2026-08-31

Purpose: revisit every major Batch01 `backend_only` / conditioned family and separate four states:

- `PROMOTABLE_EXACT_STRING` — current source contains a complete reproducible route string;
- `PROMOTABLE_CONDITIONED` — source contains a complete route/setup but position, height, spacing, state or timing must remain visible;
- `KEEP_BACKEND_INCOMPLETE` — source itself is shorthand / has missing segment / does not provide one reproducible learner string;
- `TRAINING_MODE_REQUIRED` — current source confirms the family but exact practical condition still needs lab closure.

This document does **not** promote an item merely because it exists. Pareto value and S-stage ownership still apply.

---

## Jamie

Primary current source: SF6 Lab Jamie Combos & Okizeme, Year 4.

### Can now be treated as exact route strings

1. `SA3 > 66 > 5HP(1–2 hit) > H 623K`
   - Result: returns to the familiar `+39` H Arrow package.
   - State: `PROMOTABLE_EXACT_STRING` / S4.

2. `SA3 > Raw DR > 2KK > jump > j.HK > TC`
   - Result: corner / `+4` family.
   - State: `PROMOTABLE_EXACT_STRING` / S4.

3. `2LP > 2LP > SA2 > 2MK > L Swagger Step > Swagger Hermit Punch`
   - Result: Lv4-style Hermit `+20` end-state.
   - State: `PROMOTABLE_EXACT_STRING` / S4.

4. `2MP > SA2 > 2HP > H Swagger Step > Swagger Hermit Punch`
   - State: `PROMOTABLE_EXACT_STRING` / S4.

5. `OD Swagger Step > SA2 > Raw DR > 2HP > H Swagger Step > Swagger Hermit Punch`
   - State: `PROMOTABLE_EXACT_STRING` / S4.

6. `OD Tenshin > 2HP > H Bakkai`
   - State: `PROMOTABLE_EXACT_STRING` / S4 candidate after damage/resource Pareto.

7. `OD Tenshin > 2HP > H Bakkai > SA3 > M Bakkai`
   - State: `PROMOTABLE_EXACT_STRING` / S4 kill candidate.

8. `Luminous Dive Kick > 5HP(3 hit) > L 623K`
   - Result: `+42` family.
   - State: `PROMOTABLE_CONDITIONED`; spacing / air-entry still matters.

### Keep backend

1. `move > OD Swagger Step > TC`
   - Source itself leaves starter generic.
   - State: `KEEP_BACKEND_INCOMPLETE` until starter-specific legal strings are enumerated.

2. `cancelable move > SA1 variation`
   - Source gives a real family but generic starter placeholder.
   - State: `KEEP_BACKEND_INCOMPLETE`.

### Important source correction already applied

`Bakkai = 236K family`, not `214K`. No learner asset may retain old `214K Bakkai` wording.

---

## Mai

Primary current source: SF6 Lab Mai Combos & Okizeme, Year 4.

### Can now be treated as exact route strings / setups

1. `5LK > LK > LK > j.214P | Musasabi no Mai`
   - Result: current guide gives `+41/+42` family and forward-jump HK safe-jump structure.
   - State: `PROMOTABLE_CONDITIONED`; exact `+41` vs `+42` remains route/hit-state dependent.

2. `5LKx3 > OD j.214P | Musasabi > H 623K`
   - State: `PROMOTABLE_EXACT_STRING` / high-resource route.

3. `5LKx3 > OD j.214P | Musasabi > SA2`
   - State: `PROMOTABLE_EXACT_STRING` / S4.

4. `5MP > OD 236K > j.MP > j.214P | Musasabi`
   - State: `PROMOTABLE_EXACT_STRING`; air conversion / setup.

5. `5MP > OD 236K > j.MP > SA1`
   - State: `PROMOTABLE_EXACT_STRING`; corner SA conversion.

6. `Hien Ren Kyaku > [强化状态] OD Musasabi > H 214P > H 623K`
   - Current source provides the full boosted-state string.
   - State: `PROMOTABLE_CONDITIONED` / S4 because enhancement-resource availability is mandatory.

7. `OD 214P > 5HP > H 214P > L 236K`
   - State: `PROMOTABLE_CONDITIONED`; starter, screen position and airborne state affect legality / result.

8. `2MP > OD 214P > SA1`
   - State: already learner-safe as a current Year4 full string.

### Advanced setplay now source-closed enough to store as conditioned assets

1. `4HK > HK` corner state -> approximately `+48`; `5MK whiff > L 214P` active-frame meaty.
2. H `214P` `+43` family -> `5LP x2 > 6MP` active-frame overhead.
3. SA1 normal hit `+26` -> `2LK whiff > 4HK` active-frame meaty.
4. SA1 enhanced-state `+40` -> dash -> `+22` family.

These are `PROMOTABLE_CONDITIONED`, not universal scripts. Corner/state/timing labels remain mandatory.

### Keep backend

1. `OD 214P > 5HP > OD 236P > Midare Kachousen > DR > ... > SA3`
   - Current source rendering contains a missing segment.
   - State: `KEEP_BACKEND_INCOMPLETE`; ellipsis cannot enter learner UI.

2. OD `214P` grouped multi-ender shorthand such as `L236K / H623K / SA1 / SA2 / SA3`
   - Only individually complete strings should be promoted.
   - State: family remains backend index; exact members may be promoted separately.

---

## Zangief

Primary current source: SF6 Lab Zangief Combos & Okizeme, Year 4.

### Can now be treated as exact route strings

1. `2LK > 5LP > OD Double Lariat > SA3`
   - State: `PROMOTABLE_EXACT_STRING` / S4 lethal.

2. `2LK > 5LP > CDR > 2LP > 3MP > CDR > 2MP > 3MP > SA2`
   - State: `PROMOTABLE_EXACT_STRING` / S4 high-resource.

3. `6HK PC > 3MP > OD Double Lariat > SA2 / SA3`
   - Source explicitly gives both super endings.
   - State: each ending may be stored as a separate `PROMOTABLE_EXACT_STRING` candidate.

4. `OD Double Lariat +38 > DR > 6HP active-frame meaty`
   - Block result around `+4`, then `3MP / M 360P / block`.
   - State: `PROMOTABLE_CONDITIONED`; only the `+38` KD bucket.

5. `OD Double Lariat +38 > DR > 2MK > 3MP > Double Lariat`
   - State: `PROMOTABLE_CONDITIONED` / exact `+38` bucket.

6. `OD Double Lariat +38 > DR > M 360P`
   - State: `PROMOTABLE_CONDITIONED` / exact `+38` bucket and SPD range.

7. `OD Double Lariat +38 > j.MP > 3MP > Double Lariat`
   - Source notes reversal-safety condition for slower invincible reversals.
   - State: `TRAINING_MODE_REQUIRED / PROMOTABLE_CONDITIONED`.

### Keep backend

1. `Power Stomps hit 1 > special` / `Power Stomps hit 1 > CDR`
   - Year4 first-hit cancel is confirmed, but source remains a route family rather than a final learner string.
   - State: `KEEP_BACKEND_INCOMPLETE` until valuable starter-specific follow-ups are closed.

2. Borscht Dynamite juggle family
   - Current guide contains a spacing/juggle shorthand rather than one clean reproducible starter string.
   - State: `KEEP_BACKEND_INCOMPLETE`.

3. Other Double Lariat / OD Lariat KD buckets
   - Source explicitly gives broad ranges (`+27…56`, `+38…57`).
   - State: `TRAINING_MODE_REQUIRED`; never flatten into one universal Oki number.

---

## Cammy

Primary current sources: SF6 Lab Cammy Combos & Okizeme + current Frame Data / Move List.

### Can now be treated as exact route strings

1. `2LP > 2LP > 5LK > SA1`
   - Current guide explicitly lists this SA1 route.
   - State: `PROMOTABLE_EXACT_STRING` / S4.

2. `2MK > DRC > 2HP > 5HP > H 623K (first hit) > SA3`
   - State: `PROMOTABLE_EXACT_STRING` / S4 lethal.

3. `5HP > DRC > 2HP > H 623K (first hit) > SA3`
   - State: `PROMOTABLE_EXACT_STRING` / S4.

4. `j.214K | Cannon Strike (PC) > 5LP > 5LK > 236MK`
   - Current guide gives the full string.
   - State: `PROMOTABLE_CONDITIONED`; landing spacing must allow 5LP.

5. `4MP > 4HK > forward jump > j.MP > j.214K`
   - Current guide gives the full air-conversion string (about 2000 in the guide).
   - State: `PROMOTABLE_CONDITIONED`; air/position state remains visible.

### Current frame closure for Cannon Strike family

Current frame source publishes height-dependent ranges:

- normal Cannon Strike: Block `-6 … +5`
- charged Cannon Strike: Block `0 … +5`
- OD Cannon Strike: Block `-3 … +5` in the current frame table

The practical guide separately highlights favorable/corner situations around `+1…+2` as the teachable pressure case.

Therefore learner wording remains:

> `j.214K` is **not universally plus**; only a sufficiently low / favorable landing should enter the frame-trap / throw / shimmy branch.

### Hooligan closure

`Hooligan Combination = 236P` is confirmed by current command references. Its important follow-up family includes:

- `236P -> Cannon Strike`
- `236P -> Fatal Leg Twister`
- other follow-ups / Silent Step variants

This remains a **setup / option family**, not one combo. Exact risk/reward and response timing remain `TRAINING_MODE_REQUIRED` before detailed branch rows are promoted.

### Keep backend

1. SA2 practical family
   - Current English structured guide does not provide one stable full learner string.
   - State: `KEEP_BACKEND_INCOMPLETE`.

2. Midscreen DI delayed-DR damage extension
   - Source only says a delayed `5HP Drive Rush` extension exists; full string is not rendered.
   - State: `KEEP_BACKEND_INCOMPLETE`.

3. Advanced Cannon Strike throw-tech trap timing
   - family is real; exact spacing/timing remains lab-sensitive.
   - State: `TRAINING_MODE_REQUIRED`.

---

# Batch01 consequence

The backend queue is now substantially smaller and cleaner:

- **Jamie:** SA2/SA3 and several OD Tenshin/Luminous routes are exact enough to be S4 candidates; only generic-starter families remain truly incomplete.
- **Mai:** several Musasabi / boosted / OD214P families are exact strings; the OD Kachousen -> SA3 ellipsis route remains blocked.
- **Zangief:** major SA2/SA3 cashouts are exact; Power Stomps, Borscht and non-+38 Lariat buckets remain backend/lab items.
- **Cammy:** SA1/SA3 are exact; SA2 and delayed-DR DI remain incomplete; Cannon Strike/Hooligan stay condition-led rather than memorized as universal scripts.

Next promotion rule: add only the exact/conditioned candidates that have a distinct Pareto job. Do not expose every newly closed route merely because it is now syntactically complete.
